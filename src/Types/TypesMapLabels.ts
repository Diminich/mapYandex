export type TypePropsMapLabels = {
    coords: Array<number>,
    editMode: boolean,
    showInput: boolean,
    setEditMode: (editMode: boolean) => void,
    setShowInput: (showInput: boolean) => void,
}